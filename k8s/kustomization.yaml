apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: url-shortener

resources:
  - namespace.yaml
  - configmap.yaml
  - postgres.yaml
  - api.yaml
  # secret.local.yaml применяется отдельно (не входит в kustomize, чтобы не утащить в git)
